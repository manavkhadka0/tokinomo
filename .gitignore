# Cofounder / founder-only docs — keep local, never push
cofounders/
INDEX.md
sync-cofounders.ps1

# Separate app repo (own git history)
tokinomo-frontend/

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
